# 세팅

로컬에서 K-SPY를 띄우는 전체 순서다. GitHub: https://github.com/JSap0914/k-spy

이 앱은 **공식적으로 공개된 실시간 HLS**를 한 화면에 붙인다. 골목 방범도 공개 영상과 연동 조건이 확인되면 추가 대상이다. 현재 연결된 골목 방범 실시간 영상은 없다. 경찰 내부망이나 별도 권한이 필요한 관제 화면은 이 앱의 환경 설정만으로 열리지 않는다.

로컬 폴더 이름이 `live-cctv`여도 상관없다. 공개 저장소 이름은 `k-spy`다.

## 0. 준비물

- Node 22.13 이상
- Git
- 브라우저
- (선택) 고속도로용 ITS 오픈데이터 키

국립공원, 한라산, 서울 교통, 하천 수위는 키 없이 된다. 고속도로만 키가 필요하다.

## 1. Node 확인

```bash
node -v
```

`v22.13.0` 보다 낮으면 Node를 올린 뒤 다시 한다.

## 2. 코드

이미 이 폴더가 있으면 그걸 쓴다. 새로 받을 때는:

```bash
git clone https://github.com/JSap0914/k-spy.git
cd k-spy
```

## 3. 패키지

```bash
npm install
```

실행 스크립트는 `npm run dev` → `vinext dev` 다. Next 서버를 따로 띄울 필요 없다.

## 4. 환경 파일

프로젝트 루트에 `.env.local`을 만든다. 예시는 `.env.example`에 있다.

Windows:

```bat
copy .env.example .env.local
```

macOS / Linux:

```bash
cp .env.example .env.local
```

파일 내용:

```bash
ITS_API_KEY=
```

키 없이 먼저 띄워도 된다. 고속도로 탭만 비어 있거나 공식 사이트 링크만 보인다.

`.env.local`은 Git에 올리지 않는다. 실제 키를 README, SETUP, 커밋 메시지, 이슈에 붙이지 않는다.

## 5. 고속도로 키 (필요할 때만)

1. [ITS 오픈데이터 CCTV](https://www.its.go.kr/opendata/opendataList?service=cctv)에서 이메일로 가입한다.
2. CCTV 화상자료 키를 신청한다. 승인되면 마이페이지 인증키 상세에서 `apiKey`를 복사한다.
3. `.env.local`에 넣는다.

```bash
ITS_API_KEY=발급받은_키
```

4. 개발 서버를 **다시** 켠다. 이미 켜져 있으면 끄고 `npm run dev`를 다시 한다. 키는 서버가 읽을 때만 반영된다.

ITS 키는 호출 한도가 있다. 한도를 넘기면 고속도로 목록이 비거나 영상이 안 붙는다. 한도는 ITS 마이페이지에서 확인한다.

## 6. 실행

```bash
npm run dev
```

브라우저에서 http://localhost:3000 을 연다.

끄려면 그 터미널에서 `Ctrl+C`.

다른 명령:

```bash
npm run lint
npm run build
```

`build`는 로컬 확인용이다. 이 저장소를 Codex Sites에 다시 올리지 않아도 로컬 허브는 돌아간다.

## 7. 화면

위쪽:

- `1` `2` `4`: 고른 카메라만 동시에 재생한다. 칸을 누른 뒤 목록에서 카메라를 고르면 그 칸이 바뀐다.
- `전체`: 지금 필터된 LIVE를 한 페이지에 전부 켠다. 개수 상한은 없다. 카메라가 많으면 브라우저가 무거워질 수 있다.

왼쪽:

- 검색: 장소, 공원, 도시
- 분류: 전체 / 국립공원 / 한라산 / 고속도로 / 도시 도로 / 서울 / 경기 / 방범·안전
- 목록에서 `LIVE`를 누르면 이 화면에서 재생된다.
- `링크`는 공식 사이트로 보낸다. 허브에 붙일 공개 스트림이 없을 때 그렇게 둔다.

`도시 도로`와 `서울`은 열릴 때 서울 TOPIS를 불러온다. `고속도로`는 ITS를 불러온다. `방범·안전`의 한강홍수통제소 카메라는 로컬 프록시(`/api/flood/hls/...`)로 재생한다.

## 8. 지금 되는 것

| 분류 | 제공처 | 필요한 것 |
| --- | --- | --- |
| 국립공원 | 국립공원공단 HLS | 없음 |
| 한라산 | 한라산국립공원관리소 HLS | 없음 |
| 도시 도로 / 서울 | 서울 TOPIS, 서울경찰청 교통정보센터 | 없음 |
| 고속도로 | 국가교통정보센터 ITS | `.env.local`의 `ITS_API_KEY` |
| 방범·안전 | 한강홍수통제소 하천 수위 감시 | 없음 |

서울 교통은 정적 샘플 몇 곳과, 탭을 열 때 TOPIS에서 받아 오는 실시간 목록이 같이 있다.

## 9. 아직 안 되는 것

세팅을 더 해도 지금 허브에서 바로 안 열리는 것들이다.

| 대상 | 상태 | 할 일 |
| --- | --- | --- |
| 전국 도시도로 UTIC | 키 신청은 되어 있고 승인 대기 | 결과조회에서 키가 나오면 `.env.local`에 넣고 API를 붙인다. 신청 IP와 맞아야 한다. |
| 경기도 도로 | 키는 나왔지만 `사용 중지된 키` | 중복 신청하지 않는다. 문의는 `1688-9090`. 풀려도 공개 메뉴에 CCTV 영상 API가 없으면 허브에 못 붙인다. |
| 골목 방범 | 2026-09-15 조사에서 공식 공개 실시간 스트림 미확인, 연결 0개 | 공개 시청 페이지·영상 주소·연동 조건이 확인되면 추가한다. UTIC 키만으로는 해당 영상을 이용할 수 없다. |
| 생활안전지도 | 위치만 공개 | 공식 링크만 둔다. |

`도시 도로·교차로` 칸이 영상 대신 안내 카드로 보이는 이유가 이것이다. 서울 LIVE는 같은 탭 목록 아래쪽에 있고, 전국 UTIC는 아직 키가 없다.

골목 방범을 일괄 제외하는 정책은 없다. 전국 모든 카메라의 비공개 여부를 확인한 것도 아니다. [전국CCTV표준데이터](https://www.data.go.kr/data/15013094/standard.do)는 설치 위치·목적 등 시설 정보를 제공한다. 이를 내려받아도 재생 주소가 생기지는 않는다. 조사 결과와 추가 기준은 [README](README.md#골목-방범-cctv도-볼-수-있나)에 정리했다.

## 10. UTIC / 경기가 승인되면

UTIC:

1. https://www.utic.go.kr/guide/newUtisDataWrite.do 결과조회에서 소속 `개인` + 신청 이메일로 확인한다.
2. 키가 나오면 이 프로젝트에 `UTIC_API_KEY`를 추가하고, 신청할 때 넣은 IP에서 서버가 뜨는지 확인한다.
3. 승인돼도 대상은 교통관제 CCTV다. 골목 방범은 해당 없다.

경기:

1. 키를 다시 받지 않는다.
2. 사용 중지가 풀리고, 공개 API에 CCTV HLS 항목이 생긴 뒤에만 붙인다.

비밀번호·키 원문은 이 저장소 밖에 적어 둔다. 커밋하지 않는다.

## 11. 안 될 때

- 포트 3000이 안 열린다: 같은 폴더에서 `npm run dev`가 떠 있는지 본다. 이미 떠 있으면 그 주소를 쓴다.
- 고속도로가 비었다: `.env.local` 키, 서버 재시작, ITS 한도를 본다.
- 서울 탭이 링크만 있다: TOPIS `/api/topis`가 막힌 것이다. 잠시 뒤 새로고침한다.
- 영상이 끊긴다: 허브가 짧은 끊김은 다시 붙인다. 안 돌아오면 그 칸에서 다시 연결하거나 페이지를 새로고침한다.
- `전체`가 버벅인다: 필터를 좁히거나 `1` `2` `4`로 줄인다. 상한을 걸어 두지는 않았다.

## 12. 공개 저장소

`origin`은 `https://github.com/JSap0914/k-spy.git` 이다.

푸시할 때 `.env.local`, 키, 비밀번호, 개인 이메일의 비밀번호는 넣지 않는다. 예전에 소스에 키가 들어 있던 커밋이 있으면 그 히스토리를 그대로 올리지 않는다.
